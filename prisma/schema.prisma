generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = env("DATABASE_URL")
  relationMode = "prisma"
}

// Placeholder user table, need to figure out auth
model User {
  id String @id
  discordName String
  twitchName String
  createdAt DateTime
}